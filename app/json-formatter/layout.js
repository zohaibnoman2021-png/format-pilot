export const metadata = {
  title: "JSON Formatter | Format Pilot",
  description: "Beautify, minify, and validate JSON instantly online with Format Pilot.",
};

export default function JsonFormatterLayout({ children }) {
  return <>{children}</>;
}
